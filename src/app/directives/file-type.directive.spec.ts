import { FileTypeDirective } from './file-type.directive';

describe('FileTypeDirective', () => {
  it('should create an instance', () => {
    const directive = new FileTypeDirective();
    expect(directive).toBeTruthy();
  });
});
